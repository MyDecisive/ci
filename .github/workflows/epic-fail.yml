name: EPIC FAIL LOL

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1

  notify-on-failure:
    if: failure() && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    needs: build
    uses: DecisiveAI/ci/.github/workflows/slack-blarp.yml@main
    secrets:
      SLACK_BLARP_URL: ${{ secrets.SLACK_BLARP_URL }}