name: EPIC FAIL LOL

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1

  notify-on-failure:
    # if: failure() && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    needs: build
    if: failure()
    uses: DecisiveAI/ci/.github/workflows/slack-blarp.yml@rlaw/slack-blarp
    secrets:
      SLACK_BLARP_URL: ${{ secrets.SLACK_BLARP_URL }}
    with:
      repo_name: Cool repo lul
      repo: ${{ github.repository }}
      workflow: ${{ github.workflow }}
      ref: ${{ github.ref }}
      run_id: ${{ github.run_id }}